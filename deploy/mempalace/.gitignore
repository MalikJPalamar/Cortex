# Never commit raw conversation exports or the generated extract.
exports/*
!exports/.gitkeep
mempalace-extract.jsonl

# The sample/ dir is SYNTHETIC and intentionally tracked (it contains no real
# conversation content). Generated demo output is not.
mempalace-demo-*.jsonl
